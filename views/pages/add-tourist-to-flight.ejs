<!doctype html>
<html>
<head>
  <% include ../partials/head %>
</head>
<body>
  <header>
    <% include ../partials/header %>
  </header>
  <div class="content">
    <% tourists.forEach((tourist) => { %>
        <%= tourist.name %>
        <a href="/added/<%= tourist._id %>/<%= flight._id %>">Add to flight</a> </br>
    <% }); %>
  </div>
  <footer>
    <% include ../partials/footer %>
  </footer>
</body>
</html>
